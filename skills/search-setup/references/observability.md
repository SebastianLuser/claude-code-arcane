# Observabilidad — Search Setup

- Query latency p50/p95/p99 (alertar si p95 > 200ms)
- Zero-result rate (target <5%), CTR@5 (<20% = relevancia mala)
- Index lag (outbox pendientes), failed events
- Log sampling queries 1% (sin PII)

#!/usr/bin/env bash
# Profile: +jira
# Description: Jira via curl/REST API — issues, sprints, boards
# Type: addon

DESCRIPTION="Jira via curl/REST API — issues, sprints, boards"

SKILLS=(
  jira-tickets
)

RULES_UNIVERSAL=()
RULES_GAMEDEV=()

AGENTS=()

PERMISSIONS_ALLOW=()
PERMISSIONS_DENY=()
